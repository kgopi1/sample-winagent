trigger:
- master

pool:
  name: azselfpool
  vmImage: 'az-kg-lnx-agent'

steps:
- task: Bash@3
  inputs:
    targetType: 'inline'
    script: |
      # Set ansible working dir
      cd "$HOME/ansible-windows"

- task: Bash@3
  inputs:
    targetType: 'inline'
    script: |
      # list ansible working dir
      win_playbookdir = $(System.DefaultWorkingDirectory)/windows-playbook
      ansible-playbook $(win_playbookdir)/win-main.yml


      