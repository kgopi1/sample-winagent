# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  name: azselfpool
  vmImage: 'az-kg-win-agent'

steps:
- script: echo Hello, world!
  displayName: 'Run a one-line script'

# - powershell: "New-item -itemtype Directory c:\\testdir"
#   displayName: "Create new directory step"
#   name: "Directorycreation"

- script: powershell $pwd
  displayName: Get the workdir 


- script: powershell try { cd pwsh } catch { write-host "no dir" }finally { ls }
  displayName: list the contents

- script: powershell try { install-sync.ps1 } catch {write-host "unable to install"} 
  workingDirectory: pwsh/azuread
  displayName: install-azuread
